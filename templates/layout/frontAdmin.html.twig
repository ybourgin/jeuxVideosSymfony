{% extends 'base.html.twig' %}

{% block header %}
    <nav class="navbar navbar-expand-lg navbar-light bg-danger" style="box-shadow: 0px 5px 5px grey ">
        <div class="container-fluid">
            <div style="display: flex; flex-direction: row; align-items: center">
                <a href="{{ path('home') }}">
                    <img src="{{ asset('build/image/WC.png') }}" alt="logo" width="60">
                </a>

                <li style="list-style: none" class="nav-item dropdown">
                    <a style="text-decoration: none; color: #FFFFFF; font-weight: bold" class="nav-link dropdown-toggle"
                       href="#" id="navbarDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        Menu
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('home') }}">Home</a></li>
                        <li><a class="dropdown-item" href="{{ path('forum_index') }}">Forum</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{{ path('post_category') }}">Post Category</a></li>
                        <li><a class="dropdown-item" href="{{ path('post') }}">Post</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{{ path('ticket') }}">Ticket</a></li>
                    </ul>
                </li>
            </div>
            {% if is_granted('ROLE_ADMIN') %}
                <a class="btn btn-lg btn-warning"  href="/logout">
                    deconnexion
                </a>
            {% endif %}
        </div>
    </nav>
{% endblock %}

{% block footer %}
    <div style="background-color: #dc3545; text-align: center;position:absolute; width:100%; bottom: 0; height: 80px">
        <h2 style="color: #FFFFFF">ADMIN FOOTER</h2>
    </div>
{% endblock %}